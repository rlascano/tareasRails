<%= form_for task, remote: true do |f| %>
  <%= f.check_box :completed %>
  <%= f.submit  %>
  <%= f.label :completed, task.description %>
  <%= link_to "x", task, class:"btn btn-danger",method: :delete, data: {confirm: "Seguro?"},
      remote: true %>
<% end %>
