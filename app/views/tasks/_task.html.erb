<%= form_for task, remote: true, authenticity_token: true do |f| %>
  <%= f.check_box :completed %>
  <%= f.submit  %>
  <%= f.label :completed, task.description %>
  <%= link_to "(eliminar)", task, method: :delete, data: {confirm: "Seguro?"},
    remote: true %>
<% end %>
