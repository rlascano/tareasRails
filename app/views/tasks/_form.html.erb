<%= form_for @task, id: 'new_task', remote: true do |form|  %>
  <p>
    <%= form.label :description %>
    <%= form.text_field :description %>
    <%= form.submit class:"btn btn-primary"%>
  </p>
<% end %>
